export enum QueryCode {
    GET_EXTENSIONS = "getExtensions",
}

export enum EventCode {
    CORE_READY = "coreReady",
    EXTENSION_LOAD = "extensionLoad",
    EXTENSION_UNLOAD = "extensionUnload",
}
